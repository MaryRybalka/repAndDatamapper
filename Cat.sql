create table cat (
                     cat_id integer primary key,
                     name varchar (255),
                     color varchar(255),
                     number_of_lines integer,
                     sex varchar(20)
);